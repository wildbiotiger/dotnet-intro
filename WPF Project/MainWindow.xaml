<Window x:Class="WPF_Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Project"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding Name}"/>
        <TextBox Grid.Row="1" Text="{Binding Name}"/>
        <TextBox Grid.Row="2" Text="{Binding MyProperty}"/>
        <ComboBox Grid.Row="3" ItemsSource="{Binding Components}" SelectedValue="{Binding SelectedComponent}"/>
        <Label Grid.Row="4" Content="{Binding SelectedComponent}"/>
    </Grid>
</Window>
